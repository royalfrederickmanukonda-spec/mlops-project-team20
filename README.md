# mlops-project-team20
# MLOps Project

## Objective
Build an end-to-end ML + MLOps pipeline using local open-source tools.

## Run locally (quickstart)
1. Create virtualenv & install:
   ```bash
   python -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
